interface:
  display_name: "Claude Marketplace"
  short_description: "Repo-specific patterns and workflows for claude-marketplace"
  default_prompt: "Use the claude-marketplace repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true
# Tool Surface Index (generated)

Generated by `scripts/regen-routing.sh` from the server source catalog.
Do not hand-edit; rerun the script instead. `--check` fails on drift.

- server_version: `live`
- tools: 187
- digest: `f55203a9595800cf`

## code

| Tool | Purpose |
|---|---|
| `code_compile_spec` | Compile CodeFile/CodeSymbol graph records into a CodeSpecification IR snapshot. |
| `code_extract_features` | Extract code connection feature records from a repository code graph snapshot. |
| `code_implementation_obligations` | Compile implementation obligations from a code specification, features, drift, and patterns. |
| `code_ingest` | Native CodeCrawler heavy path for ingesting or reindexing repositories. |
| `code_patterns_relevant` | Read relevant code pattern memory records for a repository and query/path prefix. |
| `code_publish_spec` | Atomically publish an immutable named CodeSpecification baseline and an admitted-identity receipt for the current repository revision. |
| `code_spec_drift` | Compare a CodeSpecification against the current repository code graph snapshot. |

## context

| Tool | Purpose |
|---|---|
| `context_explain` | Explain included, excluded, stale, and evicted Harness context generations for the authenticated project session. |
| `context_invalidate` | Idempotently advance the Harness-owned context epoch for the authenticated project session. Lifecycle hooks call this after compaction so the next turn recompiles. |
| `context_status` | Report the active Harness context generation, lease, epoch, and staleness for the authenticated project session. |

## coordination

| Tool | Purpose |
|---|---|
| `coordination_context` | Read a bundled native Theorem harness coordination context packet for turn-start injection, including ambient code and web evidence when available. |
| `coordination_contribution` | Capture an agent contribution as a durable native coordination event record. |
| `coordination_intent` | Write your live footprint for this room: what you are doing now and which files your hands are on, for peers to build on (a footprint, not a lock). |
| `coordination_record` | Write a durable native Theorem harness coordination record. |
| `coordination_room` | Join or inspect a native Theorem harness coordination room backed by THG GraphStore. |

## core

| Tool | Purpose |
|---|---|
| `browse_for_me` | Run the browser-use perceive -> afford loop for a task, bounded by policy and returning a BrowsingRun receipt. |
| `browse_with_me` | Start or advance a supervised co-browse session with pre-action preview and browser-use receipts. |
| `calibration_reliability` | Read the persisted reliability posterior, Brier score, and admission tier for one head/model/domain/claim-type cell. |
| `checkpoint` | Parse safetensors/GGUF checkpoints, compare manifests for lineage evidence, and emit CycloneDX 1.7 ML-BOMs from explicit checkpoint inputs. |
| `commitment_check` | Check generated output against active standing commitments for a scope and persist tensions for verified violations. |
| `commitment_retract` | Retract an active standing decision while retaining its graph history. The next relevant context lease recompiles without that commitment. |
| `commitment_supersede` | Supersede one active standing decision with another while preserving both records for diagnosis. |
| `composed_agent_run` | Run one composed-agent turn through the binding scratchpad and alignment gate. |
| `compute_code` | Combined CodeCrawler tool for code ingest and code fact computation: ingest, reindex, search, context, explain, recognize, explore, and fact extraction. |
| `coordinate` | Write a native Theorem harness direct-coordination message and queue @actor mentions. |
| `datawave_ingest` | DATAWAVE-style intake: turn any record into normalized field-facts plus declared entity-edges (record/batch), then read them back by value+field (lookup/intersect). Data-driven via a HelperSpec (csv/json/mapped); the engine does the normalization, not the agent. |
| `describe` | Materialize one federated affordance's full input schema on demand by affordance_id. |
| `encode` | Record a native feedback, solution, or postmortem memory with fitness metadata. |
| `evidence_bundle` | Bundle record ids and Data API query refs into a compact cited handoff packet with records, links, trace, provenance, and degraded notes. |
| `fold_semiring` | Recursively accumulate semiring scores over a derivation graph from `node_id` upward, following DERIVATION_ANCESTOR_EDGE and DERIVATION_ASSUMES_EDGE edges. `semiring_kind` selects the accumulation algebra: boolean, counting, tropical, or probability. |
| `forget` | Soft-delete a native memory document or typed memory node with an audit reason. |
| `fractal_expansion` | Queue live RustyRed fractal expansion through the async harness server route, returning a pollable run_id by default. Pass wait=true for a synchronous diagnostic receipt. |
| `handoff` | Create a native cross-actor handoff memory document. |
| `impact` | Rank blast radius across KG/code/docs/jobs/obligations from a seed record, symbol, route, file, or claim id, with Data API evidence. |
| `invoke` | Invoke a selected federated MCP affordance through its persisted connector target; use dry_run=true to plan without firing. |
| `mentions` | Read pending native Theorem harness mentions for an actor. consume=true requires write mode. |
| `observe` | Observe native harness context without writing or consuming memory state. |
| `observe_web` | Read URL/API/docs evidence from RustyWeb/Datawave records and expose the results through the Data API envelope. |
| `oracle` | Validate a claim against graph verification, records, and evidence. Can write a learn_pattern receipt only when write mode is explicit and MCP is write-enabled. |
| `plan` | Create and operate durable graph-backed plans. Plan mutations return affected counts and rule receipts; query modes are bounded and never accept raw SQL. |
| `presence` | Read, refresh, or end native Theorem harness actor presence. |
| `programmable_graph` | Validate, project, recalculate, trigger, compile, evolve, publish, or fork a typed standing program. Stochastic operators cannot reach asserting sinks without a verifier or fold boundary. |
| `programmable_graph_apply` | Atomically materialize or publish a validated typed standing program and its receipt, stream, lineage, and Block edges. |
| `query_data` | Query tenant-scoped RustyRed records through the Data API envelope. Supports record id lookups, collection/label scans, exact/contains/prefix filters, cursor paging, link hydration, provenance, rank_signals, and trace receipts. |
| `recall` | Recall native Theorem harness memory documents and graph-node memories from the tenant GraphStore. Default recall returns an abstract scan list keyed by id; use detail="overview" with detail_top_k to read summaries/previews, then pass chosen ids as detail_ids to open full content. |
| `reconstruct` | Compound reconstruction tool over reverse_engineer_compose, reconstruct_binary, datawave_ingest, and the generative reconstruction engine. Modes include compose, binary, datawave, source_to_binary, slice, behavior_ir, target_plan, emit, validate, port, and generate (mode=generate routes seeded Layer-1 facts through run_domain_pipeline for a domain: building-fixture or creature; the manifest carries fact provenance refs and a Beta-posterior-folded confidence). |
| `reconstruct_binary` | Native binary reconstruction compiler: load/analyze/lift an artifact into GraphStore and emit bounded reconstruction instructions for agents. |
| `refine` | Verification refinery compound tool: ingest a local corpus, derive span-anchored claims, compute epistemic standing, certify a signed manifest, emit PROV/DPS exports, verify artifacts, and report status. |
| `reflect` | Expand beyond current memory, synthesize with configured model heads, validate every claim against admitted evidence, and persist typed mental-model atoms with oracle receipts. |
| `relate` | Find native graph neighbors connected to a saved memory document or memory node. |
| `remember` | Write a native Theorem harness memory document or typed memory node. |
| `replay_last_run` | Deterministically replay the latest harness run for the ambient tenant and bound session/agent identity, or a same-tenant explicit run. Returns exact ledger integrity, a normalized audit timeline, raw pagination, and a content-addressed applied/refused receipt without firing effects. |
| `retrieve_memory` | Memory retrieval built on query_data: deterministic tenant/repo/path/room/tag/status narrowing, then ranked memory records inside the candidate set. |
| `rustyweb_search_acquisition` | Queue RustyWeb search provider fan-out through the async harness server route, returning a pollable run_id by default. Pass wait=true for a synchronous diagnostic result. |
| `source_calibration_record` | Record an oracle, proof, user-correction, or diagnosis outcome for one head/model/domain/claim-type source cell. |
| `spawn_session` | Spawn a Claude Code session that is room-visible and runs via the committed GitHub Actions theorem-handoff workflow (repository_dispatch), not the Railway runner. |
| `turn_start` | Return a work-queue context packet: coordination discovery, relevant memory records, open checklist records, and degraded retrieval notes. |
| `understand_code` | Explain a repo/path/query using code graph facts and Data API evidence: feature map, component map, ownership map, call graph summary, risks, and records. |
| `upsert_note` | Create or update an Obsidian-synced memory document by stable doc_id and reconcile its [[wikilink]] edges (resolved links become edges, removed links are tombstoned, forward references are recorded and resolved on target creation). |
| `why_derivation_trace` | Walk the derivation graph upward from `node_id`, collecting the ancestor chain as a WhyTrace tree showing which assumptions and intermediate derivations support the target proposition. |

## data_registry

| Tool | Purpose |
|---|---|
| `data_registry_get` | Read one tenant-scoped declarative DataRegistry entry with its immutable source manifest, content-addressed publication receipt, unresolved obligations, and recorded conflicts. |
| `data_registry_list` | List tenant-scoped declarative DataRegistry entries with bounded cursor pagination and hydrated immutable lineage. |
| `data_registry_publish` | Atomically publish an immutable, content-addressed source manifest into the tenant DataRegistry with admitted-principal lineage. Replays are idempotent; changed manifests become explicit conflict records without replacing the active source. |

## design

| Tool | Purpose |
|---|---|
| `design_audit` | Run deterministic Design Scout validators over normalized facts or dembrandt extraction JSON. |
| `design_drift` | Compare candidate design facts against a baseline and return a dembrandt-style drift report. |
| `design_extract` | Normalize static design facts or dembrandt extraction JSON into the Design Scout fact model; live browser extraction is routed through the async native browser server. |
| `design_report` | Render a self-contained HTML Design Scout report from facts, with optional baseline drift. |
| `design_tokens` | Emit design tokens from facts in W3C DTCG, Tailwind, or both formats. |

## ensemble

| Tool | Purpose |
|---|---|
| `ensemble_install` | Install or upgrade a signed Ensemble pack through its immutable version pointer. |
| `ensemble_list` | List signed Ensemble packs with lineage, recomputed trust, and install state. |
| `ensemble_publish` | Publish a signed immutable Ensemble pack version anchored to a resolution key. |
| `ensemble_register` | Register a content-addressed Ensemble capability pack in the tenant GraphStore. |
| `ensemble_select` | Select registered Ensemble capability packs under task, budget, trust, and prior constraints. |
| `ensemble_upgrade` | Install or upgrade a signed Ensemble pack through its immutable version pointer. |

## epistemic

| Tool | Purpose |
|---|---|
| `epistemic_compile_subgraph` | Compile the user's content subgraph or dirty delta for Theseus epistemic enrichment. |
| `epistemic_dirty_frontier` | Return content nodes whose EpistemicShadow is absent, explicitly dirty, or stale, expanded by k hops. |
| `epistemic_shadow_ppr` | Run personalized PageRank over EpistemicShadow nodes and shadow-layer edges. |

## graphql

| Tool | Purpose |
|---|---|
| `graphql_introspect` | Return the GraphQL SDL for the typed Harness schema. |
| `graphql_mutate` | Run a GraphQL MUTATION (write) over the typed Harness schema: the complete Plan lifecycle, Library create/index/refresh/change handling, rememberMemory / reviseMemory / forgetMemory / createHandoff, Graph designate/bulk writes, and Coordination writes. |
| `graphql_query` | Run a GraphQL QUERY (read) over the typed Harness schema: Plans, Items, Memory, Libraries, Graph, Adaptive Index Spine, Coordination, Epistemic, Code, KG, and cluster domains. Read-only: mutation operations are refused (use graphql_mutate). Tenant is the connection tenant, not a field argument. |

## harness

| Tool | Purpose |
|---|---|
| `harness_append_transition` | Append a native Theorem harness transition into the tenant GraphStore event log. |
| `harness_prepare` | Compose a native Theorem Context Brief from Ensemble selection, tenant memory recall, ambient code, and ambient RustyWeb evidence. |
| `harness_replay` | Replay durable plan transition and refusal events for one Plan. |
| `harness_run` | Read a native Theorem harness run plus ordered event ledger from the tenant GraphStore. |

## harness_kg

| Tool | Purpose |
|---|---|
| `harness_kg_explain_edge` | Explain why a merged Instant KG edge exists between two objects. |
| `harness_kg_impact` | Compute the blast radius from a code object in the Harness Instant KG merged view. |
| `harness_kg_ppr` | Run Personalized PageRank over the Harness Instant KG merged base+delta view. |
| `harness_kg_related_objects` | Find code objects related to a seed in the Harness Instant KG merged view. |
| `harness_kg_search` | Run lexical code-object search over the Harness Instant KG merged view. |
| `harness_kg_status` | Return Harness Instant KG merged-view status for the tenant base graph plus an optional session delta. |

## identity_binding

| Tool | Purpose |
|---|---|
| `identity_binding_explain` | Explain the provenance chain from the ambient principal through tenant, actor, session, binding, AgentModel, UserModel, and project-selection status. This is read-only and refuses unauthenticated identity. |
| `identity_binding_status` | Read the ambient authenticated principal and binding status. Identity comes only from verified connection claims; the receipt reports materialization, lifecycle, budgets, unresolved session/project gaps, and a content hash. |

## job

| Tool | Purpose |
|---|---|
| `job_archive` | Dispatch v2: archive a Job thread with a reason. |
| `job_list` | Dispatch v2: list the Job board ordered by priority then submitted_at, optionally filtered by repo and derived state. |
| `job_note` | Dispatch v2: append a receipt to a Job thread. Receivers may include start_session_ref for the set-once launch write. |
| `job_submit` | Dispatch v2: create or upsert a pending Job thread. Duplicate idempotency_key returns the existing job. |

## memory

| Tool | Purpose |
|---|---|
| `memory_dedup_report` | Read-only, work-bounded duplicate-candidate report over tenant MemoryDocument title and actor facts. |
| `memory_documents_dump` | Page raw MemoryDocument nodes by updated_at for sync/bootstrap lanes without compiling the full tenant graph. |
| `memory_similar_edges` | Read computed MEMORY_SIMILAR edges and referenced memory node metadata for graph and mirror surfaces. |
| `memory_similarity_reindex` | Compute MEMORY_SIMILAR edges over tenant MemoryDocument nodes using the configured embedder or the deterministic hash fallback. |
| `memory_vector_backfill` | Backfill tenant MemoryDocument embeddings, designate the vector property, and persist vectors onto graph nodes so memory recall can use vector search. |

## multihead

| Tool | Purpose |
|---|---|
| `multihead_claim` | Acquire or release a leased CAS claim on a native multi-head task node. |
| `multihead_next` | Route the next claimable node for a head from the durable multi-head work graph. |
| `multihead_patch` | Mark a claimed task as patch_proposed and bind the patch to its base commit. |
| `multihead_proof` | Run a proof command from the substrate and persist a proof receipt for a multi-head patch. |
| `multihead_refine` | Split a claimed native multi-head task into child task nodes. |
| `multihead_review` | Open or complete an adversarial verify node for a multi-head patch. |
| `multihead_run` | Start or inspect a native multi-head work-graph run. In read-only mode only action=status is available. |
| `multihead_spawn_verify` | Spawn the sibling verify node for a patch-proposed target. |
| `multihead_submit_verify` | Submit a falsification receipt for a native multi-head verify node. |
| `multihead_task` | Create a durable claimable task node in the native multi-head work graph. |

## practice

| Tool | Purpose |
|---|---|
| `practice_close_receipt` | Read the exact persisted Compound close-harvest receipt for one admitted tenant/project/run. |
| `practice_explain` | Explain persisted practice selections, outcomes, current learning states, episode receipts, and close-harvest attribution for one admitted run. |
| `practice_status` | Read the bounded receipt-backed practice status for one admitted tenant/project/run. Tenant and project come only from authenticated identity. |

## read

| Tool | Purpose |
|---|---|
| `read_intents_for_room` | Read native Theorem harness coordination intents for a room. |
| `read_messages_for_room` | Read native Theorem harness direct-coordination messages for a room. |
| `read_records_for_room` | Read durable native Theorem harness coordination records for a room. |

## resolve

| Tool | Purpose |
|---|---|
| `resolve_entities` | List bounded tenant entities with deterministic golden records, or select the entity for one record id. |
| `resolve_explain` | Explain one tenant entity and return bounded relevant proofs against the full receipt root. |
| `resolve_ingest` | Ingest bounded records through a DATAWAVE MappedHelper into a graph-owned tenant resolver. |

## reverse_engineer

| Tool | Purpose |
|---|---|
| `reverse_engineer_behavior_ir` | Lower a composed ReconstructionSpec feature slice into language-neutral behavior IR for target-language emission. |
| `reverse_engineer_compose` | Compose the reverse-engineer pipeline into a ReconstructionSpec: ensure source repo KG, compile code IR, project code into Datawave facts, and write a compose receipt. |
| `reverse_engineer_emit` | Emit a conservative PatchSet scaffold and tests from behavior IR and a target-language plan. |
| `reverse_engineer_port` | Run compose -> slice -> behavior IR -> target plan -> emit -> validate and return all artifacts. |
| `reverse_engineer_slice` | Select a bounded feature slice from a composed ReconstructionSpec, preserving source evidence and unknowns for later target-language emission. |
| `reverse_engineer_target_plan` | Lower behavior IR into a target-language/project plan with validation commands and unresolved obligations. |
| `reverse_engineer_validate` | Attach validation receipts to an emitted PatchSet; receipts are not_run until the patch is applied to a target checkout. |

## rustyred_thg

| Tool | Purpose |
|---|---|
| `rustyred_thg_epistemic_neighbors` | Traverse epistemic-typed edges (supports, contradicts, refines, etc.) with optional confidence filtering. |
| `rustyred_thg_fulltext_search` | Search a designated full-text node property. |
| `rustyred_thg_graph_explain` | Explain the bounded THG query plan without executing raw Redis. |
| `rustyred_thg_graph_index_status` | Read index health and verify drift without exposing Redis keys. |
| `rustyred_thg_graph_neighbors` | Read graph neighbors through THG adjacency indexes. |
| `rustyred_thg_graph_query` | Run a bounded graph query. Supports adjacency neighbors and exact scalar node_match. |
| `rustyred_thg_graph_schema` | Read labels, edge types, stats, and current graph-store capability notes. |
| `rustyred_thg_index_spine` | Inspect the adaptive index spine: manifests, query receipts, advisor proposals, context views, map artifacts, training runs, and redaction-safe export validation. |
| `rustyred_thg_relational_query` | Run a native relational planner query over the tenant graph snapshot. Accepts QueryIr or a GraphQL-style selection AST and returns planner trace receipts, including multimodal rankers and fusion. |
| `rustyred_thg_vector_hybrid` | Hybrid search blending vector similarity with graph proximity. Graph seeds anchor the graph-distance component. |
| `rustyred_thg_vector_search` | Run a pure vector similarity search using HNSW indexes. Returns top-k nearest nodes. |

## rustyred_thg_algorithm

| Tool | Purpose |
|---|---|
| `rustyred_thg_algorithm_communities` | Run label-propagation community detection over the tenant graph. |
| `rustyred_thg_algorithm_communities_inline` | Run label-propagation community detection over inline adjacency (stateless; does NOT touch any tenant). Bounded by RUSTY_RED_MAX_INLINE_EDGES. |
| `rustyred_thg_algorithm_components` | Run connected-components over the tenant graph. |
| `rustyred_thg_algorithm_components_inline` | Run connected-components over inline adjacency (stateless; does NOT touch any tenant). Bounded by RUSTY_RED_MAX_INLINE_EDGES. |
| `rustyred_thg_algorithm_pagerank` | Run global PageRank over the tenant graph. |
| `rustyred_thg_algorithm_pagerank_inline` | Run power-iteration PageRank over inline adjacency (stateless; does NOT touch any tenant). Bounded by RUSTY_RED_MAX_INLINE_EDGES. |
| `rustyred_thg_algorithm_ppr` | Run Personalized PageRank over the tenant graph. |
| `rustyred_thg_algorithm_ppr_inline` | Run Personalized PageRank over inline adjacency (stateless; does NOT touch any tenant). Bounded by RUSTY_RED_MAX_INLINE_EDGES. |

## rustyred_thg_graph_version

| Tool | Purpose |
|---|---|
| `rustyred_thg_graph_version_checkout` | Reconstruct a graph snapshot from a branch name or commit hash in a caller-supplied graph repository. |
| `rustyred_thg_graph_version_compile` | Compile the tenant graph into a content-addressed Prolly-tree graph pack. |
| `rustyred_thg_graph_version_diff` | Diff a base graph snapshot against the tenant graph or an explicit target snapshot. |
| `rustyred_thg_graph_version_log` | Walk graph commit history from a branch name or commit hash in a caller-supplied graph repository. |
| `rustyred_thg_graph_version_merge` | Three-way merge graph snapshots with content-hash conflict detection and confidence-weighted edge resolution. |
| `rustyred_thg_graph_version_ref` | Compile the current tenant graph and update a branch ref inside a caller-supplied graph repository value. |

## rustyred_thg_spatial

| Tool | Purpose |
|---|---|
| `rustyred_thg_spatial_bbox` | Search a designated spatial property within a bounding box. |
| `rustyred_thg_spatial_radius` | Search a designated spatial property within a radius in kilometers. |

## rustyred_thg_symbolic

| Tool | Purpose |
|---|---|
| `rustyred_thg_symbolic_datalog_derive` | Run the parity-gated symbolic Datalog derivation over an inline fact pack. Bounded by RUSTY_RED_MAX_SYMBOLIC_FACTS. |
| `rustyred_thg_symbolic_probabilistic_expected_value` | Run the parity-gated expected-value-of-information receipt. |
| `rustyred_thg_symbolic_probabilistic_source_reliability` | Run the parity-gated Beta-Bernoulli source reliability receipt. |

## self

| Tool | Purpose |
|---|---|
| `self_archive` | Archive a native memory document into the cold memory tier. |
| `self_note` | Write a typed native self-memory document for the current actor. |
| `self_recall_archive` | Recall archived native memory atoms without returning them in default recall. |
| `self_revise` | Create a revision-tracked replacement for a native memory document. |

## skill

| Tool | Purpose |
|---|---|
| `skill_apply` | Apply a native Theorem harness skill pack and persist a use receipt. |
| `skill_get` | Read one native Theorem harness skill pack by id or content hash. |
| `skill_list` | List native Theorem harness skill packs stored in RustyRed. |
| `skill_publish` | Publish a content-addressed skill pack into native Theorem RustyRed. |

## stream

| Tool | Purpose |
|---|---|
| `stream_ack` | Acknowledge explicit-policy stream deliveries so they are not redelivered. |
| `stream_publish` | Append an event to a (tenant, topic) coordination stream. Returns its monotonic ordering token. urgency ask|block with a target_actor also pings that actor (mention + wake). |
| `stream_read` | Pull the append-only coordination events after your stored cursor on your subscribed streams (or explicit streams[]). The passive, cursor-delta read that replaces the room poll; advance=true (default) consumes the window once. |
| `stream_subscribe` | Subscribe an actor to a coordination stream so stream_read returns its delta. Returns the updated subscription set. |
| `stream_unsubscribe` | Unsubscribe an actor from a coordination stream. Returns the remaining subscription set. (A ping still reaches an unsubscribed target via mentions.) |

## tool

| Tool | Purpose |
|---|---|
| `tool_result_fetch` | Fetch a byte slice from a tool result that exceeded the MCP boundary budget. |
| `tool_search` | Search tenant-scoped federated MCP affordances through the connector gateway without advertising every spoke tool schema upfront. |

## typed_commitment

| Tool | Purpose |
|---|---|
| `typed_commitment_affirm` | Atomically affirm a typed commitment against a governing rule and evidence. Ownership is bound to the admitted principal and replays are idempotent. |
| `typed_commitment_explain` | Explain one canonical typed commitment from its persisted lifecycle receipts, including active, superseded, or retracted status. |
| `typed_commitment_read` | Read one canonical typed commitment owned by the admitted principal. Tenant and owner are derived from authenticated identity, never caller arguments. |
| `typed_commitment_retract` | Atomically retract the admitted principal's active typed commitment and retain the policy rule, evidence, and explanation in its lifecycle. |
| `typed_commitment_supersede` | Atomically supersede the admitted principal's active typed commitment while preserving immutable prior state and policy evidence. |

## verification

| Tool | Purpose |
|---|---|
| `verification_allocate` | Rank verification candidates by persisted source miscalibration, uncertainty, and supplied stakes. |
| `verification_explain` | Explain one canonical verification receipt, preserving support, attack, lineage, verifier, method, graph-version, and calibration evidence. |
| `verification_frontier_receipt` | Read one immutable tenant-scoped verification frontier, including obligation discharges, refusals, conflicting evidence, and graph versions. |
| `verification_frontier_record` | Atomically persist a deterministic epistemic frontier over bounded obligations and canonical verification receipts. Requires an admitted principal. |
| `verification_obligation_discharge` | Evaluate one bounded verification obligation against a canonical receipt and durably preserve either its discharge or exact refusal reasons. Requires an admitted principal. |
| `verification_receipt` | Read one tenant-scoped canonical verification receipt and calibration delta by content address. |
| `verification_record` | Atomically persist one canonical verification receipt, its supporting and attacking evidence lineage, and any calibration outcome. |

## web

| Tool | Purpose |
|---|---|
| `web_consume` | Navigate, observe, extract, and optionally ingest one web page through the async THG browser-use route. |
| `web_query` | Query tenant-scoped RustyWeb DATAWAVE page facts and hydrate matching page nodes. |
| `web_search_graph` | WEB arm of the context membrane (SPEC-CONTEXT-MEMBRANE-1.0): fan a query across search providers, gate the fresh + warm-substrate pools through the token budget, persist deferred handles byte-exact (recoverable), emit a membrane receipt, and warm the top result pages fire-and-forget. Returns the admitted context, deferred handles, and a stable subgraph reference. |

